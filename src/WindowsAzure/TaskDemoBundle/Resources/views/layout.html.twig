<!DOCTYPE html>
<html>
    <head>
        <title>Task Management</title>
        <link rel="stylesheet" href="{{ asset("bundles/windowsazuretaskdemo/bootstrap/css/bootstrap.min.css") }}" type="text/css" />
    </head>

    <body style="margin-top: 80px;">
        <div class="navbar navbar-fixed-top">
            <div class="navbar-inner">
                <div class="container">
                    <a class="brand" href="{{ path('task_list') }}">Windows Azure Demo</a>

                    {% if app.security.isGranted('ROLE_USER') %}
                    <ul class="nav">
                        <li><a href="{{ path('task_new') }}">Create Task</a></li>
                        <li><a href="{{ path('azure_logout') }}">Logout</a></li>
                    </ul>
                    {% endif %}
                </div>
            </div>
        </div>

        <div class="container">
            <div class="row">
                {% block content %}{% endblock %}
            </div>
        </div>
    </body>
</html>
